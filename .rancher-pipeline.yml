stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: blueforest/import:1.0.23
      pushRemote: true
      registry: index.docker.io
timeout: 60
